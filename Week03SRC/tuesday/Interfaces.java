package tuesday;

public class Interfaces {
	/*
	 * when you have have a header without a body it is known as an abstract method
	 * use impliments (just like extend) to use abstract methods.
	 * when you are using extends it only uses one parent and you can't pull from multiple parents
	 * 
	*/
}
